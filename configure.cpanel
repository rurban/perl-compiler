#!/bin/sh

export PATH=/usr/local/cpanel/3rdparty/perl/520/bin:$PATH

set -e

find lib/B/C/ -name "*.pm" | xargs -n1 perl -Ilib -c

#git clean -dxf
perl Makefile.PL installdirs=vendor
make -j4 install
